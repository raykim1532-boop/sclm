{
  "name": "raymond-scheduler-web",
  "version": "1.0.0",
  "private": true,
  "description": "나의 스케줄러 — Cloudflare Pages 배포 (정적 앱 + Functions API + D1)",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && wrangler pages dev public --d1 DB=scheduler-db --compatibility-date=2024-09-23",
    "deploy": "node build.js && wrangler pages deploy public --project-name raymond-scheduler",
    "db:init:local": "wrangler d1 execute scheduler-db --local --file=schema.sql",
    "db:init": "wrangler d1 execute scheduler-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
